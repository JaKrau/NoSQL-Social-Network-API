/* TODO: 
connection = require('../config/connection'); 
{ Users } = require('../models');

connection.once('open', async () => {
    await connection.dropDatabase("thisDB");

    const users = [
        {
            username:
            email:
        },
        {
            username:
            email:
        },
        {
            username:
            email:
        },
    ];

await Users.collection.insertMany(users);

console.log("You may now reap that which you've sown");

process.exit(0);
});
*/

